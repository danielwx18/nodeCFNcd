node {

stage('Create Utopia ECS'){



     withAWS(region:'us-east-1',credentials:'aws-credentials') {

                  def identity=awsIdentity();//Log AWS credentials
				  
					sh label: '', script: 'aws --region us-east-1 cloudformation create-stack --stack-name nodecfntestCD --template-url https://s3.amazonaws.com/utopia-node/swaggerYl2.yml'

    }
 }
 
}